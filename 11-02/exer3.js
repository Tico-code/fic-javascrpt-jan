const age = 6;
const isAgeFirst = age <=7;
const isAgeLast = age >=65;

if (isAgeFirst || isAgeLast) {
console.log(`Para você, a entrada é grátis! wow`)
} else {
    console.log(`A entrada é 30 trilhões de dólares`)
}